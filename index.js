var util = require('gulp-util'),
    fs = require('fs');

const PLUGIN_NAME = 'gulp-notes';

function Notes (options) {
  var opts = options || {};
  console.log( file.path );
};

module.exports = Notes;